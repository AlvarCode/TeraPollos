<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TeraPollos"
             x:Class="TeraPollos.App">
    <Application.Resources>
        <ResourceDictionary>
            <Thickness x:Key="PagePadding">24</Thickness>
            
            <!-- Colors -->
            <Color x:Key="AccentColor">#5466cf</Color>
            <Color x:Key="Primary">#f3f4f9</Color>
            <Color x:Key="Secondary">#e6e7eb</Color>
            <Color x:Key="Tertiary">#dddee1</Color>
            <Color x:Key="TextColor">#222223</Color>
            <Color x:Key="PlaceholderColor">#444448</Color>

            <Color x:Key="White">#FFF</Color>
            
            <!-- Styles -->
            <Style TargetType="ContentPage"
                   ApplyToDerivedTypes="True">
                <Setter Property="BackgroundColor"
                        Value="{StaticResource Primary}" />
                <Setter Property="Padding"
                        Value="{StaticResource PagePadding}" />
            </Style>

            <Style TargetType="Label">
                <Setter Property="FontSize"
                        Value="16" />
                <Setter Property="TextColor"
                        Value="{StaticResource TextColor}" />
            </Style>

            <Style x:Key="Title"
                   TargetType="Label">
                <Setter Property="FontSize"
                        Value="28" />
                <Setter Property="HorizontalTextAlignment"
                        Value="Center" />
            </Style>

            <Style x:Key="Subtitle"
                   TargetType="Label">
                <Setter Property="FontSize"
                        Value="22" />
            </Style>

            <Style TargetType="Button">
                <Setter Property="BackgroundColor"
                        Value="{StaticResource AccentColor}" />
                <Setter Property="TextColor"
                        Value="{StaticResource Primary}" />
            </Style>
            
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
